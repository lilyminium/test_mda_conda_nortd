name: test_mda_conda_nortd-test
channels: 
  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # MDAnalysis
  

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist

  # Pip-only installs
  - pip:
    - codecov
    - MDAnalysis

